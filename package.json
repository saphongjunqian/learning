{
  "name": "acfinance",
  "version": "1.0.3",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4",
    "@sap/xssec": "^4",
    "@cap-js/hana": "^1"
  },
  "devDependencies": {
    "@cap-js/cds-typer": "^0.25.0",
    "@cap-js/sqlite": "^1",
    "@sap/ux-specification": "^1.120.12",
    "rimraf": "^5.0.7"
  },
  "scripts": {
    "start": "cds-serve",
    "build": "rimraf resources mta_archives && mbt build --mtar archive",
    "deploy": "cf deploy mta_archives/archive.mtar --retries 1",
    "undeploy": "cf undeploy accounts --delete-services"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "db/acfinance.sqlite"
        }
      },
      "[production]": {
        "auth": "xsuaa",
        "db": {
          "kind": "hana-cloud",
          "impl": "@cap-js/hana"
        }
      }
    }
  },
  "sapux": [
    "app/accountcategories",
    "app/accounts",
    "app/organizations",
    "app/transactiontypes",
    "app/documents"
  ]
}
